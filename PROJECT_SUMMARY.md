# 🤖 Анализ кодовой базы AtermTrackBot

Проект **AtermTrackBot** представляет собой многофункциональный асинхронный Telegram-бот для логистического мониторинга. Он объединяет отслеживание дислокации контейнеров, статусы терминалов, управление поездами и расчет Ж/Д расстояний. Бот автоматически импортирует данные из почтовых вложений, предоставляет информацию по запросу и рассылает кастомизированные отчеты.

---

## 🚀 1. Основная логика и Точка входа

| Файл | Описание | Основные операции |
| :--- | :--- | :--- |
| `bot.py` | **Точка входа** в приложение. Инициализирует `Application` (`python-telegram-bot`), устанавливает логирование, регистрирует все диалоги (`ConversationHandler`), команды и колбэки. Выполняет критическую инициализацию БД и запускает планировщик в `post_init`. | `main()`: Сборка и запуск бота. `post_init()`: Инициализация БД, установка команд меню, запуск планировщика. `set_bot_commands()`: Установка команд для пользователей и администратора. |
| `config.py` | Содержит все **настройки** и переменные окружения, загружаемые из `.env` (токены, URL БД, ID админа, настройки почты, константы отчетов). | `DATABASE_URL`, `TELEGRAM_TOKEN`, `ADMIN_CHAT_ID`, `IMAP_SERVER`, `SMTP_HOST` и др. |
| `scheduler.py` | Управляет **фоновыми задачами** бота через `AsyncIOScheduler` (часовой пояс Владивостока). | `start_scheduler()`: Регистрирует задачи (рассылка отчетов, проверка почты на файлы дислокации и терминала). `job_send_notifications()`: Запускает рассылку отчетов по подпискам. `job_periodic_dislocation_check()`: Периодическая проверка почты на файлы дислокации. |
| `logger.py` | Настраивает **систему логирования** для всего проекта. | `get_logger()`: Создает и возвращает логгер с настроенными обработчиками (консоль и ротируемый файл), обеспечивая единообразный формат логов. |

---

## 💾 2. База данных и ORM

| Файл | Описание | Основные ORM-модели |
| :--- | :--- | :--- |
| `db.py` | Настраивает **асинхронный движок SQLAlchemy** (`asyncpg`) и фабрику сессий (`async_sessionmaker`) для взаимодействия с PostgreSQL. | `init_db()`: Создает все таблицы в БД, определенные через `Base`. `SessionLocal`: Фабрика для создания асинхронных сессий. |
| `db_base.py` | Предоставляет декларативный базовый класс `Base` для всех ORM-моделей SQLAlchemy. | `class Base(DeclarativeBase):`. |
| `models.py` | Определяет основные **ORM-модели** для таблиц пользователей, подписок, дислокации, поездов и правил уведомлений. | `User`, `UserEmail`, `Subscription`, `Tracking` (для дислокации), `Train` (агрегированный статус поезда), `EventAlertRule` (правила уведомлений о событиях), `StationsCache`, `TrainEventLog`, `VerificationCode`. |
| `model/terminal_container.py` | Определяет ORM-модель `TerminalContainer` для хранения "сырых" данных о контейнерах на терминале (клиент, поезд, статус, дата приема). | `class TerminalContainer(Base)`: Используется импортерами и для построения отчетов по поездам. |
| `queries/train_queries.py` | Инкапсулирует **запросы к БД, связанные с поездами**. | `upsert_train_on_upload`: Создает/обновляет поезд при загрузке файла. `update_train_status_from_tracking_data`: Обновляет статус поезда из данных дислокации. `get_train_details`: Получает детальную информацию о поезде. `get_latest_active_tracking_for_train`: "Умный" поиск последней актуальной дислокации для поезда. |

---

## 💌 3. Обработчики (Handlers)

| Файл | Описание | Основные функции и взаимодействие |
| :--- | :--- | :--- |
| `handlers/dislocation_handlers.py` | Обрабатывает текстовые сообщения с **номерами контейнеров/вагонов** для получения мгновенной дислокации. | `handle_message()`: Извлекает номера, логирует запрос, ищет дислокацию, рассчитывает расстояние. Формирует подробный текстовый отчет для одного контейнера или Excel-файл для нескольких. |
| `handlers/tracking_handlers.py` | Управляет **диалогом создания подписки** на отслеживание. | `add_subscription_start`: Начало диалога. `ask_emails`: Запрашивает выбор Email-адресов из списка пользователя. `save_subscription`: Сохраняет `Subscription` и связывает его с выбранными `UserEmail`. |
| `handlers/subscription_management_handler.py` | Управляет **просмотром и удалением** существующих подписок (`/my_subscriptions`). | `my_subscriptions_command()`: Выводит список подписок с кнопками управления. `delete_subscription_callback()`: Удаляет подписку по нажатию кнопки. |
| `handlers/email_management_handler.py` | Управляет **Email-адресами** пользователя (`/my_emails`) и их **подтверждением по коду**. | `add_email_receive`: Получает адрес, генерирует и отправляет код подтверждения по почте. `receive_verification_code`: Проверяет код и активирует Email в БД. |
| `handlers/train.py` | Управляет **отчетами по поездам** и **загрузкой файлов поезда** (только для админа). | `train_cmd`: Точка входа, показывает список поездов. `_respond_train_report`: Формирует отчет с клиентами (`TerminalContainer`) и последней дислокацией (`Train`). `handle_train_file`: Обрабатывает загруженный Excel-файл поезда, запуская импорт. |
| `handlers/distance_handlers.py` | Управляет **диалогом расчета тарифного расстояния** между станциями (`/distance`). | `process_to_station()`: Получает от пользователя станции отправления и назначения, вызывает `services.tariff_service.get_tariff_distance` и отправляет результат. |

---

## ⚙️ 4. Сервисы (Services)

| Файл | Описание | Основные функции и взаимодействие |
| :--- | :--- | :--- |
| `services/dislocation_importer.py` | Отвечает за **импорт данных дислокации** из Excel-файлов РЖД, полученных по почте. | `check_and_process_dislocation()`: Скачивает файл по IMAP. `process_dislocation_file()`: Читает Excel, приводит типы, выполняет **UPDATE/INSERT** в `Tracking`. `update_train_statuses_from_tracking()`: Агрегирует данные и обновляет таблицу `Train`. |
| `services/terminal_importer.py` | Отвечает за **импорт данных о статусах терминала** (A-Terminal) и файлов поезда. | `check_and_process_terminal_report()`: Ищет и скачивает отчет A-Terminal. `process_terminal_report_file()`: Обрабатывает отчет, обновляя `TerminalContainer`. `import_train_from_excel()`: Обрабатывает загруженный админом файл поезда, проставляя номер поезда и клиента в `TerminalContainer`. |
| `services/notification_service.py` | Основной сервис для **рассылки уведомлений** по подпискам и событиям. | `send_scheduled_notifications()`: Находит подписки, собирает дислокацию, отправляет сообщение в Telegram, генерирует Excel и отправляет Email. `send_aggregated_train_event_notifications()`: Рассылает уведомления о событиях поезда (прибытие, отправление). |
| `services/imap_service.py` | Синхронный сервис для **подключения к IMAP** и скачивания вложений. Используется через `asyncio.to_thread`. | `download_latest_attachment()`: Ищет письмо по заданным критериям (отправитель, тема, имя файла), скачивает вложение и помечает письмо как прочитанное. |
| `services/tariff_service.py` | **Асинхронный адаптер** для вызова ядра расчета Ж/Д расстояний. | `get_tariff_distance()`: Вызывает синхронный `zdtarif_bot.rail_calculator.get_distance_sync` в отдельном потоке, чтобы не блокировать основной цикл `asyncio`. |
| `services/file_utils.py` | Утилита для **работы с файлами Telegram**. | `save_temp_file_async()`: Асинхронно скачивает файл по `file_id` (например, загруженный админом Excel-файл поезда) и сохраняет его во временную папку для дальнейшей обработки. |
| `services/train_event_notifier.py` | Логика **обнаружения и регистрации событий поезда** на основе данных дислокации. | `process_dislocation_for_train_events()`: Анализирует новые данные дислокации, сравнивает их с предыдущим состоянием и, если находит значимое событие (прибытие, отправление), записывает его в `TrainEventLog`. |

---

## 🛠️ 5. Утилиты (Utils)

| Файл | Описание | Основные функции |
| :--- | :--- | :--- |
| `utils/send_tracking.py` | Генерация **Excel-файлов** для отчетов. | `create_excel_file()`: Создает Excel-файл из списка строк с данными дислокации, применяет форматирование (ширина колонок, цвета). `get_vladivostok_filename()`: Генерирует имя файла с датой и временем по Владивостоку. |
| `utils/email_sender.py` | Синхронная отправка Email через SMTP. Используется через `asyncio.to_thread`. | `send_email()`: Подключается к SMTP-серверу и отправляет письмо с текстом и вложениями. `generate_verification_email()`: Формирует HTML-тело письма с кодом подтверждения. |
| `utils/railway_utils.py` | Работа с **железнодорожными кодами и названиями дорог**. | `get_railway_abbreviation()`: Извлекает код дороги из строки (например, `(51)`) и возвращает её общепринятое сокращение (например, `С-КАВ` из `СЕВЕРО-КАВКАЗСКАЯ (51)`). |

---

## 🛤️ 6. Подсистема расчета тарифов (`zdtarif_bot`)

Эта директория содержит логику для расчета тарифных расстояний. Она может функционировать как отдельный бот, но в данном проекте используется как библиотека.

| Файл | Описание | Основные функции |
| :--- | :--- | :--- |
| `zdtarif_bot/telegram_bot.py` | **Точка входа для отдельного бота** "Инфо ЖД Тариф". В `AtermTrackBot` не используется напрямую. | `main()`: Запускает автономного бота для расчета расстояний. |
| `zdtarif_bot/rail_calculator.py` | **Синхронное ядро** расчета тарифного расстояния. Загружает справочники (`2-РП.csv`, `3-1 Рос.csv`, `3-2 Рос.csv`) при старте. | `get_distance_sync()`: **Ключевая функция**, используемая в `AtermTrackBot`. Синхронно выполняет поиск станций и расчет расстояния по матрицам транзита. |
| `zdtarif_bot/core/` | Пакет с основной логикой калькулятора. | `data_loader.py`: Загружает и кэширует CSV-файлы со справочниками. `data_parser.py`: Содержит функции для нормализации имен станций. `calculator.py`: Реализует алгоритм поиска кратчайшего пути (Дейкстра) по графу станций. |