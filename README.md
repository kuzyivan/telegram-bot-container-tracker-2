# AtermTrackBot: Telegram-бот для логистического мониторинга

**AtermTrackBot** — многофункциональный асинхронный Telegram-бот для комплексного отслеживания логистических операций.
Он объединяет дислокацию контейнеров, мониторинг поездов и данных по терминалам. Бот автоматически импортирует данные из почтовых вложений, предоставляет информацию по запросу и рассылает кастомизированные отчеты по гибкой системе подписок.

---

## 📋 Оглавление
- [🚀 Ключевой функционал](#-ключевой-функционал)
- [🛠️ Технологический стек](#️-технологический-стек)
- [📊 Архитектура проекта](#-архитектура-проекта)
- [🔧 Установка и запуск](#-установка-и-запуск)
- [⚙️ Настройка окружения](#️-настройка-окружения)
- [🛤️ Подсистема расчета тарифов](#️-подсистема-расчета-тарифов)

---

## 🚀 Ключевой функционал

### Для пользователей

*   **Гибкая система подписок (`/my_subscriptions`)**:
    *   Создание неограниченного количества списков отслеживания.
    *   Персональные имена для каждой подписки.
    *   Индивидуальное управление каждой подпиской.
    *   Настройка времени получения ежедневных отчетов.
*   **Управление Email-адресами (`/my_emails`)**:
    *   Добавление и хранение списка email-адресов.
    *   Выбор адресов для получения Excel-отчетов по каждой подписке.
*   **Простой поиск дислокации**:
    *   Мгновенное получение информации о местоположении контейнеров по их номерам.
*   **Расчет расстояний (`/distance`)**:
    *   Расчет тарифного расстояния между железнодорожными станциями.

### Для администраторов

*   **Массовые рассылки (`/broadcast`)** всем пользователям бота.
*   **Экспорт данных (`/stats`, `/tracking`)** — выгрузка статистики и активных подписок.
*   **Управление поездами**:
    *   Просмотр отчетов по поездам.
    *   Загрузка файлов для обновления информации о поездах.
*   **Тестирование рассылок**:
    *   `/testnotify` — тестовый отчет для администратора.
    *   `/force_notify [ЧЧ:ММ]` — принудительный запуск рассылки для всех пользователей.

---

## 🛠️ Технологический стек
- **Язык:** Python 3.11+
- **Telegram API:** [python-telegram-bot v20+](https://docs.python-telegram-bot.org/)
- **База данных:** PostgreSQL
- **ORM:** SQLAlchemy 2.0+ (асинхронный драйвер `asyncpg`)
- **Миграции:** Alembic
- **Планировщик задач:** APScheduler
- **Работа с почтой:** imap-tools / smtplib
- **Excel:** pandas + openpyxl
- **Веб-фреймворк:** FastAPI
- **Конфигурация:** python-dotenv
- **Логирование:** logging

---

## 📊 Архитектура проекта

Проект построен по многоуровневой архитектуре:

-   `handlers/` — обработка команд и сообщений от пользователей.
-   `services/` — реализация основной бизнес-логики (импорт данных, создание и отправка уведомлений).
-   `queries/` — инкапсуляция всех запросов к базе данных через SQLAlchemy ORM.
-   `models.py`, `model/` — описание таблиц базы данных (ORM-модели).
-   `scheduler.py` — управление фоновыми задачами.
-   `web/` — веб-интерфейс на FastAPI.
-   `utils/` — вспомогательные функции (создание Excel, отправка email, и т.д.).
-   `bot.py` — точка входа в приложение, инициализация и запуск бота.

---

## 🔧 Установка и запуск

### 1. Предварительные требования
- Python 3.11+
- PostgreSQL
- `virtualenv` (рекомендуется)

### 2. Установка
```bash
git clone <repository_url>
cd telegram-bot-container-tracker-2

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Настройка окружения
Создайте файл `.env` в корне проекта и заполните его по примеру ниже.

### 4. Миграции базы данных
Примените миграции для создания таблиц в базе данных:
```bash
alembic upgrade head
```

### 5. Запуск бота
```bash
python bot.py
```

---

## ⚙️ Настройка окружения

Пример файла `.env`:

```ini
# --- Telegram ---
TELEGRAM_TOKEN="ВАШ_ТЕЛЕГРАМ_ТОКЕН"
ADMIN_CHAT_ID="ВАШ_ТЕЛЕГРАМ_ID"

# --- PostgreSQL (DSN) ---
# Для Alembic (синхронный драйвер)
ALEMBIC_DATABASE_URL="postgresql+psycopg2://user:pass@host:port/db"
# Для бота (асинхронный драйвер)
DATABASE_URL="postgresql+asyncpg://user:pass@host:port/db"

# --- Почта для импорта дислокации/терминала ---
IMAP_SERVER="imap.example.com"
EMAIL="bot-email@example.com"
PASSWORD="пароль_приложения_для_imap"

# --- Почта для отправки отчётов ---
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="bot-email@example.com"
SMTP_PASS="тот_же_пароль_приложения"

# --- Настройки веб-сервера ---
WEB_HOST="0.0.0.0"
WEB_PORT="8000"
```

---

## 🛤️ Подсистема расчета тарифов (`zdtarif_bot`)

Проект включает в себя подсистему `zdtarif_bot` для расчета тарифных расстояний между железнодорожными станциями. Эта логика используется в команде `/distance`. Хотя она может функционировать как отдельный бот, в данном проекте она интегрирована как библиотека.